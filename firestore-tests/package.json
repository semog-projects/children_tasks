{
  "name": "children-tasks-firestore-tests",
  "private": true,
  "type": "module",
  "description": "Testes das Security Rules do Firestore (issue #6)",
  "scripts": {
    "test": "node --test \"**/*.test.mjs\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^4.0.1",
    "firebase": "^11.3.0"
  }
}
